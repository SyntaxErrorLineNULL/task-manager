/**
 * Author: SyntaxErrorLineNULL.
 */

export enum Keys {
  SEND_EMAIL = 'notify.sendEmail',
}

export enum Types {
  QUEUE = 'queue',
  EXCHANGE = 'exchange',
}

export enum ExchangeTypes {
  TOPIC = 'topic',
  DIRECT = 'direct',
}
